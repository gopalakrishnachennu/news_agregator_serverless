# =============================================================================
# Environment Configuration Template for Tembo Hosting
# =============================================================================
# Copy this file to .env and fill in your actual credentials
# NEVER commit .env files with real credentials to version control
# =============================================================================

# -----------------------------------------------------------------------------
# DATABASE CREDENTIALS (Tembo Hosted PostgreSQL)
# -----------------------------------------------------------------------------
# Get these values from your Tembo dashboard or using:
#   tembo instance list
#   tembo instance info <instance-name>

# For Go services (feed-collector, web-scraper) and TypeScript services (indexer)
# Format: postgres://<username>:<password>@<host>:5432/<database>?sslmode=require
DATABASE_URL=postgres://your_user:your_password@your-instance.tembo.io:5432/your_database?sslmode=require

# For Python services (sentiment-engine, translation-engine)
DB_HOST=your-instance.tembo.io
DB_USER=your_user
DB_PASS=your_password
DB_NAME=your_database
DB_PORT=5432

# -----------------------------------------------------------------------------
# KAFKA CONFIGURATION
# -----------------------------------------------------------------------------
KAFKA_BROKER=kafka:29092
KAFKA_TOPIC=new-urls

# -----------------------------------------------------------------------------
# MINIO / OBJECT STORAGE CONFIGURATION
# -----------------------------------------------------------------------------
MINIO_ENDPOINT=minio
MINIO_PORT=9000
MINIO_ACCESS_KEY=minio_user
MINIO_SECRET_KEY=minio_password

# -----------------------------------------------------------------------------
# OPTIONAL SERVICE CONFIGURATION
# -----------------------------------------------------------------------------
POLL_INTERVAL_MINUTES=5
CLUSTER_DISTANCE_THRESHOLD=0.22
CLUSTER_TIME_WINDOW_HOURS=24
TEXT_SIMILARITY_THRESHOLD=0.45
SENTIMENT_MODEL=cardiffnlp/twitter-roberta-base-sentiment-latest
