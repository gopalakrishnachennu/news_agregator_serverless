# =============================================================================
# Local Development Environment Configuration
# =============================================================================
# Use this for local development with docker-compose
# Copy to .env.local and adjust as needed
# =============================================================================

# -----------------------------------------------------------------------------
# DATABASE CREDENTIALS (Local Docker PostgreSQL)
# -----------------------------------------------------------------------------
# These connect to the local postgres container defined in docker-compose.yml

# For Go services (feed-collector, web-scraper) and TypeScript services (indexer)
DATABASE_URL=postgres://news_user:news_password@postgres:5432/news_db?sslmode=disable

# For Python services (sentiment-engine, translation-engine)
DB_HOST=postgres
DB_USER=news_user
DB_PASS=news_password
DB_NAME=news_db
DB_PORT=5432

# -----------------------------------------------------------------------------
# LOCAL POSTGRES CONTAINER SETTINGS
# -----------------------------------------------------------------------------
POSTGRES_USER=news_user
POSTGRES_PASSWORD=news_password
POSTGRES_DB=news_db

# -----------------------------------------------------------------------------
# KAFKA CONFIGURATION
# -----------------------------------------------------------------------------
KAFKA_BROKER=kafka:29092
KAFKA_TOPIC=new-urls

# -----------------------------------------------------------------------------
# MINIO / OBJECT STORAGE CONFIGURATION
# -----------------------------------------------------------------------------
MINIO_ENDPOINT=minio
MINIO_PORT=9000
MINIO_ACCESS_KEY=minio_user
MINIO_SECRET_KEY=minio_password
MINIO_ROOT_USER=minio_user
MINIO_ROOT_PASSWORD=minio_password

# -----------------------------------------------------------------------------
# OPTIONAL SERVICE CONFIGURATION
# -----------------------------------------------------------------------------
POLL_INTERVAL_MINUTES=5
CLUSTER_DISTANCE_THRESHOLD=0.22
CLUSTER_TIME_WINDOW_HOURS=24
TEXT_SIMILARITY_THRESHOLD=0.45
SENTIMENT_MODEL=cardiffnlp/twitter-roberta-base-sentiment-latest
