# =============================================================================
# Tembo Cloud Production Environment Configuration
# =============================================================================
# Use this for deploying to production with Tembo-hosted PostgreSQL
# Copy to .env.tembo and fill in your Tembo credentials
#
# IMPORTANT: Use sslmode=require for production security
# =============================================================================

# -----------------------------------------------------------------------------
# DATABASE CREDENTIALS (Tembo Hosted PostgreSQL)
# -----------------------------------------------------------------------------
# Get these values from:
#   Option 1: Tembo Cloud Dashboard (https://cloud.tembo.io)
#   Option 2: Tembo CLI:
#     tembo instance list
#     tembo instance info <instance-name>
#
# Your Tembo host will look like: <instance-name>.<org-id>.tembo.io

# For Go services (feed-collector, web-scraper) and TypeScript services (indexer)
# IMPORTANT: Use sslmode=require for Tembo production
DATABASE_URL=postgres://<username>:<password>@<your-instance>.<org-id>.tembo.io:5432/<database>?sslmode=require

# For Python services (sentiment-engine, translation-engine)
DB_HOST=<your-instance>.<org-id>.tembo.io
DB_USER=<username>
DB_PASS=<password>
DB_NAME=<database>
DB_PORT=5432

# -----------------------------------------------------------------------------
# KAFKA CONFIGURATION
# -----------------------------------------------------------------------------
# Update these if using managed Kafka in production
KAFKA_BROKER=kafka:29092
KAFKA_TOPIC=new-urls

# -----------------------------------------------------------------------------
# OBJECT STORAGE CONFIGURATION
# -----------------------------------------------------------------------------
# Update these if using cloud storage (S3, etc.) in production
MINIO_ENDPOINT=minio
MINIO_PORT=9000
MINIO_ACCESS_KEY=<your-access-key>
MINIO_SECRET_KEY=<your-secret-key>

# -----------------------------------------------------------------------------
# OPTIONAL SERVICE CONFIGURATION
# -----------------------------------------------------------------------------
POLL_INTERVAL_MINUTES=5
CLUSTER_DISTANCE_THRESHOLD=0.22
CLUSTER_TIME_WINDOW_HOURS=24
TEXT_SIMILARITY_THRESHOLD=0.45
SENTIMENT_MODEL=cardiffnlp/twitter-roberta-base-sentiment-latest
